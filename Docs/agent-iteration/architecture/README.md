# Agent 架构与技术选型（参考层）

> 状态日期：2026-07-31
> 文档性质：**方向与选型参考**，不是 OpenSpec change，**不授权直接改业务代码**
> 状态：**C1–C9 能力底座已归档**（2026-08-08）；2026-08-09 蓝图 v2.0 转向核心体验与信任兑现，本目录继续作为已实现 Agent 架构参考
> 执行硬规则仍以 `AGENTS.md` → `.ai/ACTIVE_TASK.md` → active OpenSpec → baseline specs 为准

## 本目录解决什么问题

蓝图管「往哪走、什么顺序」，OpenSpec change 管「这一刀做什么、怎么验收」。两者中间缺一层：
**哪些分层与端口必须长期守住，才能在换检索引擎 / 换 provider / 换评测方案时不推倒 Runtime。**
本目录补这一层：

1. **架构宪法**：逻辑分层、六大稳定端口、Loop 不变式、反推倒底线
2. **技术选型草稿**：已落地事实 + ADR 式推荐 + 明确不选 + 实现漂移登记

## 文件

| 文件 | 用途 | 状态 |
|---|---|---|
| `agent-architecture-constitution.md` | 分层、端口、不变式、反推倒原则、气质硬边界 | v0.2（已按 Phase 1 归档事实校准） |
| `tech-selection-draft.md` | 选型事实、ADR、漂移登记、跨 change 待确认清单 | v0.2（已按 Phase 1 归档事实校准） |

## 与蓝图的关系

| 文档 | 角色 |
|---|---|
| `roadmap/iteration-blueprint.md` | **v2.0 已冻结**（2026-08-09）；当前核心体验与信任序列 |
| `roadmap/iteration-blueprint-v1.2.md` | 历史只读；C1–C9 能力与工程 ADR 的方向来源 |
| 本目录 | 支撑蓝图的架构与选型依据；**只冻结原则，不冻结类名** |
| `narrative/agent-tech-story.md` | 对外叙事（面试向）；本目录的 ADR 是它的素材源 |

**分工边界**：蓝图冻结**能力与顺序**；本目录冻结**分层与端口语义**；
类名、表名、检索算子、阈值在各 change 的 `design.md` 定，并回写本目录的漂移表。

## 维护节奏

```text
每刀 change 归档后
  → 更新宪法 §2.1「现状锚点」表，删除过时类名
  → 在选型草稿 §6 追加漂移行（仅当实际选择与原假设不一致且有意保留）
  → 关闭选型草稿 §7 中已决的待确认项
蓝图版本升级时
  → 消化漂移表，同步本目录状态头与阶段对齐章节
```

**当前进度**：Phase 1（C1 → C2 → C4 → C3a → C3b → C5）与 Phase 2（C6 → C7 → C8 → C9）全部归档，端口与能力底座已成形。
当前 v2.0 蓝图不预设新的 Agent 框架扩张；先做真实产品表面、此刻片段与数据主权，P4.1/P4.2 再对齐见证者与记忆主动权。

## 禁止

- 用本目录替代 OpenSpec 验收契约
- 把本目录的 `planned` 项当作已批准 scope 直接写代码
- 写入 API key、用户日记原文、本机秘密路径
- 在无独立 Type C 批准的情况下翻案 §5「明确不选」清单中的项
