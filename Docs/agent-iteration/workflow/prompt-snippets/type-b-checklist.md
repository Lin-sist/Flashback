# Type B 小修操作性清单

> 用途：低风险维护的可勾选步骤。  
> 判定拿不准 → 按 Type C 或先问用户。硬禁止见 `AGENTS.md`。

---

## 事前闸门

- [ ] 确认不属于 Type C（未改 API/权限/持久化/AI 语义/用户可见主能力契约）
- [ ] 若存在 active Type C：确认本修 **不** 与其核心文件恶性冲突；冲突则请示
- [ ] 输出 **待改清单**：路径 + 现状 + 建议改法
- [ ] 写明提交责任：`用户手动提交`（默认）或 `Agent 提交`
- [ ] **用户确认前不改代码**

## 执行

- [ ] 只改清单内文件；不做顺手重构
- [ ] 运行与风险相称的验证（或写 SKIPPED+原因）
- [ ] 追加 `.ai/AGENT_LOG.md`（结构化模板；大白话一句改前/改后可选）
- [ ] 输出 `AGENTS.md` Required Output 字段

## 收尾

- [ ] 按提交责任处理 commit；默认不 push
- [ ] 不创建空 OpenSpec change，不把 ACTIVE_TASK 改成无关状态
