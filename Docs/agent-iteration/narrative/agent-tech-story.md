# Flashback Agent 技术叙事（对外向）

> 文档性质：**对外叙事**（面试 / 作品集 / 技术分享）——**不是** OpenSpec 契约，也不授权改任何代码
> 状态日期：2026-08-03（§8 随 C7 `agent-reflection-loop` 归档补写）
> 上游事实源：`roadmap/iteration-blueprint.md` v1.2 §9、`architecture/*`、`openspec/specs/**`、各刀 archive
> 维护纪律（D33）：**每刀 change 归档时更新对应段落**，不是最后一次性补写
>
> **硬边界（隐私等级最高）**：本文档是最可能被复制到外部的文件。
> **禁止**出现用户日记原文、真实 secret、本机绝对路径、真实用户可识别信息。
> 约束与 `AgentTraceCollector` 同源：只写结构化事实与数值。

## 本文档怎么用

按**面试问题**组织，不按代码分层组织。每节结构固定：

- **一句话答案** — 先给结论
- **怎么做的** — 实现要点
- **为什么这样** — 取舍与代价
- **证据** — 可指向的代码/测试/数据

进度：§1–§9、§11 已写（C1–C8 已完成）；§10 持续追加。

---

## §1 一分钟版本

**项目是什么**：《时光回序》Flashback，一个微信小程序日记产品。用户写下当下的情绪与困惑，把回答权交给时间，未来的自己重新理解这一刻。产品气质是安静、私密、克制——不是效率仪表盘，不是社交流，不是心理诊断工具。

**Agent 在其中承担什么**：一个被动召唤的共情友人。用户主动点开它才出现，帮用户把说不清的感受一层层问出来（情绪 → 困惑 → 核心问题 → 期望），记得用户三个月前写过什么，可以建议加标签或补正文但绝不代替用户封存。技术上是一个后端托管的单 Agent loop，不是前端调 API 的单轮问答。

**双项目分工**：我有两个 AI 项目，刻意分工——

| 项目 | 证明什么 |
|---|---|
| RAG 项目 | AI **知道**什么：检索、分块、可信、评测 |
| Flashback | AI **能做**什么：Loop、Tool、Memory、Guardrail、Trace、Eval、Reflection、Resilience |

**技术栈**：Spring Boot 3.3.5 + MyBatis + MySQL 8.0 + Uniapp/Vue3 小程序；AI 侧走 OpenAI-compatible HTTP（DeepSeek），Agent runtime 全自研。

**当前进度**：Phase 1 六刀与 Phase 2 前三刀已归档（Eval → Reflection → Resilience）；下一刀是 Temporal。C8 的最终回归数字与环境跳过项见归档 closeout。

---

## §2 为什么自研 Loop，而不用 LangGraph / Spring AI

**一句话答案**：因为我的产品语义锁在阶段机里，而框架能给我的两样东西——可视化和 checkpoint——前者我不需要，后者我已经有了。

**怎么做的**

`AgentStageMachine` 是一个纯逻辑类（无 IO、无持久化、无 provider 调用）：

- 线性序列：`EMOTION → CONFUSION → CORE_QUESTION → EXPECTATION → CLOSING → ENDED`
- 两条抢占边：用户明确结束意图（关键词命中）、轮次达上限 → 直接进 `CLOSING`
- 一条自环：回避型极短回答时同阶段追问，**上限 1 次**，避免逼问
- `REVIEW`（友人回看）刻意不经此机器，传进来直接抛异常快速失败

**推进权在后端，不交给模型。**这是刻意的：「追问上限」「强制收束」「不逼问」必须能被单测钉死，交给模型就变成概率行为。

**为什么这样**

严格说，**这已经是一张图**——节点、边、抢占、自环、终态都在，只是用 `List` + `indexOf` 表达而不是 `Node`/`Edge` 类。所以「引入图框架」对我的增量收益只有：

| 框架收益 | 我的情况 |
|---|---|
| 通用节点图与可视化 | 六个节点的线性流程，不需要 |
| durable execution / checkpoint | 已有：会话与消息落 MySQL，可 resume ACTIVE 会话 |
| 社区示例 | 换不来产品语义 |

代价则是重写一套已测的阶段语义。**结论：留了 Port，没留框架。**

Spring AI 同理。我的请求形状已经是 industry shape(OpenAI-compatible messages + tools），框架是实现便利，不是能力解锁。而且 Spring AI 2.0 要求 Spring Boot 4.x + Framework 7，迁移得先做一次平台代际升级，风险叠乘。**更关键的一点**：迁移会同时吃掉我的 ToolRegistry、MemoryPort、护栏挂点、轨迹采集点——而这些恰好是我唯一能讲清楚也能逆向学习的部分。用框架换掉它们，我就从「作者」退化成「使用者」。

**我承认的代价**：自研意味着重试、多模态、流式这些都得自己写。如果哪天需要它们中的多数，迁移窗口才真正打开。

**证据**：`AgentStageMachine`（纯逻辑 + `AgentStageMachineTest`）、`agent_session` / `agent_message` 表、`AgentModelClient`。

---

## §3 工具调用怎么做的，为什么不做假降级

**一句话答案**：原生 function calling + 模型白名单门控 + 二段式确认；FC 不可用就退回纯对话，**绝不自己发明一套提议协议**。

**怎么做的**

- 协议用 provider 原生 FC，内部有 `AgentToolSchemaFactory` 生成 schema、`AgentToolValidator` 校验参数
- 可用性由**配置化的模型白名单**门控，不假设任意 OpenAI-compatible 模型都支持 FC
- schema strict 模式表达不了的约束（如正文最大长度）在代码层补
- 写操作**必须用户确认**：模型只能提议，前端弹确认条，用户点了才执行
- 工具执行结果（observation）回注下一轮上下文，窗口大小可配

**为什么这样**

「FC 不可用时用 prompt 模拟一套提议协议」是很常见的降级思路，我明确否决了。理由：那条路径会产出一个**看起来成功、实际是我自己解析出来的**工具调用。一旦它和真实 FC 混在同一个出口，我就无法回答「这次到底走的哪条路」。这撞在项目的一条硬规则上——**真实路径不得 mock success 冒充真实成功**。

**不做 MCP 内核化**：MCP 在 2026 已是工具互操作的事实标准，但我的工具是内生业务 API（草稿、标签、封存），没有外部消费方。现在 MCP 化只增加一层转换和一片安全面。所以 ToolRegistry 保持内部契约，MCP 列为**可选适配层**——需要时在 Registry 外面包一层，而不是反客为主。

**证据**：`agent/tool/*`(Registry / Coordinator / Validator / Executor）、`AgentModelClientToolCallingTest`、`agent_tool_call` 表。

---

## §4 护栏怎么判，为什么不用 LLM 审核官

**一句话答案**：确定性检查 + 忠实度双指标 + 按路径分流的降级；判定不交给第二个模型。

**怎么做的**

六类违规，每类带一个结构化短标识：

| 违规 | 判什么 |
|---|---|
| `UNFAITHFUL` | 增写了用户没表达过的内容 |
| `FABRICATED_QUOTE` | 引号里的「你说过」在原话中无出处 |
| `DIAGNOSTIC` | 在自己新增的表述里下诊断或给医学建议 |
| `FAKE_ACTION` | 谎称已代替用户封存 / 解锁 / 删除 |
| `MISSING_TIME_ATTRIBUTION` | 复述历史内容却没说清那是过去的事 |
| `CHECK_ERROR` | 判定器自身异常 → fail-closed |

**忠实度用双指标**：覆盖比例 + 最长连续未覆盖片段。单看比例会漏掉「大部分照抄、中间塞一句编造」;单看片段长度会误伤正常改写。两个一起才拦得住。

**降级按路径分流**，因为三条路径的失败成本不同：

- 工具提议 → **拒绝**(用户只是少看到一个确认条）
- 素材草稿 → **丢弃**(可选产物，复用「生成失败即 null」语义）
- 对话回复 → **替换为本地常量兜底**(用户提交了一轮，必须得到回应）

失败方向是刻意选的：**宁可少一个确认条、少一段素材，也不放行一句用户没说过的话进他的日记。**

兜底回复是本地常量，且必须留下结构化痕迹使其可与真实回复区分——**兜底不得伪装成模型正常输出**。

**为什么这样**

不用 LLM 审核官，三个具体原因：

1. **延迟**：实测 provider 单次 avg 6476ms。生成 + 审核 + 重写 ≈ 19.5s，直接撞死后端 20s 超时。
2. **可复现性**：LLM 判定不可复现，而我现在这套判定被几百个测试钉死，给定输入必然得到确定 violation。用不确定的换确定的，是净损失。
3. **隐私**：把用户回复连同原话送去给第二个模型打分，属于未授权外发。

**这里最容易被追打的一点我先说**：确定性检查能判「有没有越界」，判不了「好不好」。「听起来这段时间很难熬」和「你这是抑郁症的典型症状」的区别我能判（后者命中 `DIAGNOSTIC`）；但「这句共情够不够真诚」我判不了。**我没有假装能判**——见 §7。

**补充一处 C6 实测到的边界**：「只在新增区段匹配」这个性质的成立条件比直觉窄——它依赖 4-gram 覆盖，因此 Agent 复述用户说过的病名时，**是否连带复用了周边措辞**会决定放行还是拦截。同一句用户输入下只改复述措辞就会翻转。这是 n-gram 方案的固有性质，也是当初刻意选的误伤方向（代价是用户收到一句兜底回复，而不是错误内容外发）。C6 把它写成一条用例钉住现状，**没有借机改阈值**。

**隐私设计**：判定结果 `AgentGuardrailVerdict` 是一个 record，只含 violation 类型 + 覆盖比例 + 最长未覆盖长度 + 受检字符数。**类型层就不接受文本内容**，因为它要进审计和日志。有一条专门的测试断言指标串不含原文片段。

**证据**：`agent/guardrail/*`、`AgentGuardrailBoundaryCaseTest`（五场景，每组含正例反例）、`verdictMetricsMustNotLeakContent`。

---

## §5 记忆分层怎么落地，为什么不上向量库

**一句话答案**：`MemoryPort` 抽象 + MySQL 关键词/标签/时间检索 + 有预算的注入；向量作为同一 Port 下的 v2 插件，不作为前提。

**怎么做的**

术语对齐业界的 multi-layer memory:

| 层 | 在我这里是什么 |
|---|---|
| Working | 当前会话的滑动消息窗口 |
| Episodic | 用户历史记录（检索命中的片段） |
| Semantic | 记录上的结构化摘要字段 |

检索：从近期消息抽关键词（有最小长度与数量上限）+ 标签 + 时间回溯窗口 → 命中片段。

**注入有预算**：条数上限、单片段字符上限，全部可配。不是「有多少历史塞多少」。

**注入的片段带时间元数据**，并且配了一条专门的护栏 `MISSING_TIME_ATTRIBUTION`：如果 Agent 复述了三个月前的内容却不说清那是过去的事，读起来就像用户刚说的话——**等于把三个月前的心情冒充成此刻的心情**。这是我在做记忆时最在意的一个失效模式，危害具体而非抽象。

**为什么这样**

不上向量库的理由不是「不会」，是**规模不匹配**：当前记录量级用 SQL 检索足以验证「它记得你」这个产品命题。提前上云向量库会让隐私边界、成本、本地开发、测试夹具全部变重，而换来的相关性提升在这个数据量上无法被证明。

所以做成 `MemoryPort`:`search(userId, query, filters, budget) → List<MemoryHit>`。检索从 LIKE → FULLTEXT → 旁路 embedding 都是同一个 Port 下换实现，不需要动编排层。

**我承认的短板**：现在是标签 + 说明性字段 LIKE,**没有权重、没有分词、没有向量**，相关性弱。这一条我在残留清单里明确记着（R9），没有粉饰成「够用了」。

**严格用户隔离**：所有检索按 userId 收口，不跨用户，后台也没有「全局扫描日记」的任务。

**证据**：`agent/memory/*`、`MySqlMemoryPortTest`、`MemoryCueExtractorTest`、注入预算配置项。

---

## §6 可观测性做了什么，发现了什么

**一句话答案**：每轮对话落一条 versioned 决策轨迹（thought → action → observation），不含任何原文；上线第一天就用它定位到一个让每轮卡满 50 秒的自锁缺陷。

**怎么做的**

一轮对话内的七八个采集点散落在编排的不同分支里。**我没有让每处各自 insert**，而是用一个 `AgentTraceCollector` 先收在内存，最后由唯一出口落库。理由很具体：provider 失败、护栏降级、fail-closed 丢弃这些**早退路径**最容易漏掉埋点，而那正是最需要痕迹的时刻。

采集的内容（全部是数值与枚举）：

- 阶段：from / to / reason
- 记忆：enabled / failed / hasCue / retrievedCount / injectedCount / injectedChars
- 上下文：messageCount + 各补充段是否存在
- provider:model / durationMs / mocked / success
- 工具：returned / proposed / discarded / rejectReason
- 护栏：每层 passed / violation / coverage / maxUncoveredRun
- 降级：layer / violation / fallback 是否本地常量
- 裁剪：beforeLength / afterLength

**隐私是靠类型系统守的，不是靠自觉**：收集方法只接受基础类型和既有枚举，**想把日记原文传进轨迹，在编译期就做不到**。少数 String 参数承载的是配置值、异常类名或结构化常量。

**版本锚点由内容哈希派生**：prompt 模板和护栏规则的内容变了，`prompt_version` / `policy_version` 自动变。这样「改文案前后的行为差异」第一次可以按版本分组比对。

**发现了什么（这部分是真正的收获）**

1. **provider 耗时首次有数据**：min 4571 / avg 6476 / max 8467ms。这项数据在此之前**完全不存在**——成功路径的起始时间被直接丢弃，只有失败路径才算耗时。有了它，我才发现前端 30s / 后端 20s 的超时设置里，后端留给 provider 的余量只有三倍中位数。

2. **一个我自己引入的严重缺陷**：轨迹落库用了 `REQUIRES_NEW` 想着「不影响外层事务」，结果每轮对话卡满 50 秒。根因：`REQUIRES_NEW` **挂起外层事务但不释放它已持有的锁**，新事务经外键去碰同一批 `agent_session` 父行，就和自己的外层事务死等到锁超时。

   **最值得说的是它怎么逃过测试的**：H2 上 37 项测试全绿。H2 没有 InnoDB 行级锁语义，**这个缺陷在 H2 上不可能复现**。修法不是换传播级别，是改用事务提交后的回调做副作用。

3. **一条反直觉的教训**：轨迹写入本身是 fail-open 的，失败不影响业务。但它在失败**之前**先卡了 50 秒。**fail-open 只保证不报错，不保证不阻塞**——评估「辅助设施失败会不会影响用户」时，必须同时看失败前的等待成本。

**诚实标注**：回看模式下的 fail-closed 分支（模型在无工具模式仍返回提议时丢弃），9 轮真实观察中**从未活体触发**，正确性目前只由单测覆盖。我把它记为未验证，没有写成「已验证」。

**证据**：`agent/trace/*`、`agent_turn_trace` 表、9 条排查 SQL、`AgentTraceCollectorTest`。

---

## §7 怎么保证改了 prompt 不退化

**一句话答案**：23 条离线用例断言**轨迹级不变量**（硬失败）+ 比对**快照指标**（需人确认），全程零外调；语言质量不假装能评，它靠人评锚定。

**为什么必须先有量尺——这件事我踩过**

R2「引导话术生硬」在待办里挂了一段时间。后来修掉一个让每轮卡满 50 秒的锁等待缺陷，用户反馈「自然一些了」——而那一轮**一行 prompt 都没改**。之前的判断样本被 55 秒延迟污染了，「话术生硬」的感觉里混着「等太久了」的感觉，当时没人察觉。

这件事的教训不是「手验不可靠」，而是**没有量尺时，连自己在测什么都不知道**。所以在动 prompt 之前先建量尺，而不是反过来。

**断言什么：轨迹信号，不是回复措辞**

这是整个设计的关键取舍。断言最终回复的文本，等于把评测绑死在模型的语言输出上——换个 model 就全红，而且看不出问题出在哪一步。断言轨迹信号则不同，它们是**编排逻辑的产物**，与模型语言能力无关：

阶段判定序列（from/to/reason）、同阶段追问次数、记忆三态、注入条数与字符、护栏各层 verdict、降级发生在哪一层、回复长度与长度比、provider 调用次数。

**两层，失败语义刻意不同**

| 层 | 回答的问题 | 失败语义 |
|---|---|---|
| 不变量 | 「这还对吗」 | **硬失败，不提供任何刷新手段** |
| 快照 | 「相对基线变了吗」 | 需人确认；变化可能完全合理 |

分层的理由：改了引导话术之后指标变化是**预期**的，把它判成缺陷会让人学会忽略红灯；但「同阶段追问超过 1 次」「注入超预算」「该降级没降级」这类事变了就是 bug，没得商量。

**怎么防快照沦为橡皮图章（这是这类框架最常见的死法）**

红了就把数字改成当前值、绿灯拿到、「为什么变了」从此无人知晓。三道机制拦它：

1. 每条基线必须带 `baselineNote`：由哪一刀定、当时什么状态、**为什么长这样**。缺了拒绝加载。
2. checksum 由**指标 + 说明共同派生**。只改数字不改说明 → 对不上 → 失败。手法和版本锚点用内容哈希同源：把「人必须记得同步」变成「不同步就报错」。
3. **不提供 `-Dupdate` 自动重写开关**。有了它，「评测红了」的最短路径就从「停下来想想」变成「跑个命令让它变绿」，整套框架退化成耗时的空转。手工编辑的摩擦在这里是特性。

第 2 条我测了两个方向。「只改数字→checksum 变」是显然的；**「只改说明→checksum 也必须变」容易被漏掉，但少了它机制是残缺的**——若只把指标纳入指纹，那么「改数字+顺手改说明」这个**正确**流程与「只改数字」会算出同一个值。

还有一条反射测试禁止出现 `write` / `save` / `update` / `rewrite` / `accept` 命名的方法。写在文档里的禁令挡不住一个赶时间的下午，一条测试可以。

**替身只替边界，不替判断**

评测里替换的只有两处：数据库 mapper 和 provider 的 HTTP 边界。护栏、状态机、上下文组装、记忆检索的截断收口、工具参数校验**全部用生产实现**。

这一条不是洁癖。举个具体例子：如果用一个假的 MemoryPort 返回我自己写的 3 条片段，那么「注入不超预算」这条断言验的就是我填的数字，而不是检索实现真的会按上限收口。**替身替掉的越多，被覆盖的生产代码越少**——极端情况下就是在断言自己的替身。

同理，provider 替身是**继承生产类只覆写两个真正发网络请求的方法**，「配置是否可用」「model 是否在工具白名单内」这些判定仍走生产逻辑。整体 mock 会把它们一并 stub 掉。

**一个不明显的坑：走 mock 分支就评不到上下文组装**

生产代码在 `if (isMockProvider())` 处**直接返回**，压根不组装 prompt。也就是说最直觉的做法——用内置的 mock 响应器跑评测——会同时缺掉上下文组装维度，而且**产不出任何降级轨迹**（内置响应器的文案都是合规的）。

所以替身刻意走「非 mock」分支。判断它是否真的走到了，判据是轨迹里有没有 `prompt` 步骤，并且**它的 messageCount 要与 provider 边界实际收到的条数一致**——对不上就说明采集点和真实调用脱节了。

**为什么不用 LLM-as-Judge（三条独立成立）**

1. **隐私**：日记原文送第二个模型打分属未授权外发，意味着**每跑一次评测都要走一次外调授权**——而「每次改动都能跑」是这套东西的全部价值，两者直接矛盾。
2. **预算**：真实调用的历史量级是「6 次 / 预算 10」。Judge 跑几十条用例即爆表。
3. **不可复现**：同一份输入两次打分可能不同，而评测的前提是维度稳定。

**必须自己说清的边界**

用确定性替身跑评测，评的是**编排逻辑**，不是模型的语言质量。这句话如果不说清，这套东西就变成一个「看起来测了质量其实没测」的资产——那比没有它更危险，因为它会让人误以为改 prompt 是安全的。

诚实表述是：**覆盖轨迹不变量与回归比对；语言质量靠真实探针的小样本人评锚定，没有假装用 Judge 覆盖它。** 人评锚点的结构已经就位但**内容为空**（填它需要真实产出 + 人工评判，属外调范围），而且「空不等于已覆盖」这句话由测试守着——空文件最大的风险就是被误读成已完成。

**另外两处如实标注**：这个仓库**没有 CI**，所以交付的是「一条 maven 命令可跑」，不是 CI 门槛；快照指标在真实 provider 下的稳定性未验证。

**顺带发现的一处护栏边界**

评测建起来之后立刻有了产出。护栏规则只在「Agent 新增区段」匹配，因此用户自己说过的病名，Agent 复述时不该被判违规。实测发现这个性质的成立条件**比直觉窄**：它取决于复述时是否连带复用了周边措辞（覆盖判定基于 4-gram）。同一句用户输入下，只改复述措辞就会从放行翻成拦截。

这是 n-gram 方案的固有性质，也是当初刻意选的误伤方向（后果是用户收到一句兜底回复，而不是错误内容外发）。**我没有借机改阈值**——建量尺和用量尺改参数是两件事，混在一起就分不清指标变化是哪个原因造成的。那正是 R2 那次误判的同型错误。我把这个边界写成一条用例钉住：它将来变宽还是变窄，都会有人知道。

**证据**：`agent/eval/*`（九个类 + 五个测试类）、`eval/cases/*.yaml` 23 条用例、`eval/baseline/snapshots.yaml` 23 条基线。后端 606 tests PASS / 4 skipped，其中 70 项为本刀新增；**生产代码零改动**。

---

## §8 反思环：为什么有环、环的预算与代价

**一句话答案**：我没有让模型“想一想自己写得好不好”，而是在确定性护栏发现一个可恢复、窄定义的问题时，
由后端只允许它重写一次；这让线性编排第一次有了受控自环，但没有把控制权交给模型。

**怎么做的**：初次 reply 仍走原生成与护栏链。只有非 `CLOSING` reply 命中
`MISSING_TIME_ATTRIBUTION` 时，`AgentReflectionPolicy` 才把 violation enum 映射成固定要求，
`AgentReplyPipeline` 再调用一次 provider。第二次调用不带 tools、关闭 strict tool calling，
并重新跑完整 reply 护栏；仍不合格就回到既有本地安全兜底。轨迹仍是一轮一条、同一 attempt，
只在 provider steps 标出 `initial` 与 `reflection`，不记录候选文本。

**为什么最终只做 reply-only**：规划时曾考虑 material 的 `UNFAITHFUL`。实现前复核发现
`CLOSING` 一轮本来就要生成 reply 与 material，已经是两次 provider 调用；material 再重写会变成三次。
按 C5 实测平均 6476ms 推算约 19.4s，几乎吃满后端 20s 上限。于是范围收窄：material 继续直接丢弃，
`CLOSING` reply 也不开环，保证单轮最多两次调用。`CHECK_ERROR`、严重越界和 provider error 同样不开环，
因为它们分别代表判定不可信、内容风险过高或应由 C8 处理的韧性问题。

**为什么不用 LLM 自检器或图框架**：自检器会把“生成 + 自检 + 重写”推到约 19.5s，
还用一个不确定判断替换已有确定性 checker；图框架则没有带来当前需要的并行分支或汇合能力。
一个封闭 eligibility 集合、一次上限和明确终态，比引入新的运行时更容易审计。

**证据**：C7 新增 policy / pipeline 与脱敏 trace phase；C6 合成用例由 23 增至 28，
后端 **622 tests PASS / 4 skipped**。真实 MySQL、真实 provider 与真机 C7 活体验收未执行，
因此这里只能证明编排与边界按设计工作，不声称真实模型重写质量已经验证。

---

## §9 韧性：失败必须可解释，但不能把基础设施暴露给用户

**一句话答案**：我把 provider 失败收敛为 8 类稳定类型，让一次编排中的所有模型调用共享
24000ms deadline，并坚持零自动 retry；用户看到的是按阶段映射的克制提示，系统留下的是无内容轨迹。

**怎么做的**：`AgentModelClient` 在 HTTP、解析与线程中断边界按异常类型和 status 分类，核心逻辑
不解析异常 message 或 response body。`AgentCallBudget` 让 initial / reflection / material 共用一份
单调时钟预算，每次 timeout 取 `min(20000ms, remaining)`；剩余不足时直接形成类型化失败，不发下一请求。
失败不会持久化本地 Assistant 假回复，既有用户消息仍保留，用户可从原入口主动重试同一 turn。

**为什么不自动重试**：C7 已允许一次受控 reflection，`CLOSING` 又可能依次生成 reply 与 material。
再加隐式 retry 会让调用数和尾延迟不可审计，也可能在 30s 前端窗口之外才返回。C8 因此只标记
`transient` 供排查，不把它变成自动控制信号；多 provider、熔断、缓存与监控均留给独立 change。

**用户体验与隐私**：opening、普通 turn、closing/material 只按 operation / stage / category 选择固定文案，
不展示 HTTP status、provider、endpoint、鉴权或异常类。trace 只保留 phase、category、transient、
budget 桶与终态，不记录 prompt、用户日记、候选回复、provider body 或 exception message。

**证据**：离线分类、fake clock、编排、trace、契约与 eval 回归全部通过；闸门 3 使用固定合成短文本，
真实 DeepSeek 共 6/6 次成功——两次单 canary 为 1378ms、1656ms，两组双调用为 2898ms、3531ms。
真实 MySQL 证明失败后主动重试仍是同一 turn、仅一条 USER message，attempt 1 为
`UNAVAILABLE/auth-configuration`、attempt 2 为 `SUCCESS`。这些是小样本验收，不是生产 SLA；
本机没有可控微信开发者工具/真机环境，因此真机 UI 证据明确为 SKIPPED。

---

## §10 我知道但没做的，及理由

> 这一节是刻意写的。行业面试会考「什么时候某种编排是多余的复杂度」——**能论证不做，和能做出来同等重要。**

| 技术 | 不做的核心理由 | 翻案条件 |
|---|---|---|
| 图框架（LangGraph4j 等） | 现有状态机已具备节点/边/抢占/自环/终态；框架增量收益是可视化（不需要）与 checkpoint（已由 MySQL 会话实现） | 出现真正的并行分支与合并需求 |
| Spring AI 迁移 | 契约已是 industry shape;它要求 Boot 4.x + Framework 7，须先做平台代际升级；迁移会吃掉 Tool/Memory/Guardrail/Trace 四处自研资产 | 需要多模态 + 流式 + 重试中间件，自维护成本明显超过迁移成本 |
| LLM-as-Judge | 三条独立成立：日记原文外发需授权（于是每跑一次评测都要走一次授权，与「每次改动都能跑」直接矛盾）+ 真实调用预算在个位数量级而 Judge 跑几十条即爆表 + 打分不可复现违反「维度稳定」 | 有独立的、脱敏的、可复现的评测通道 |
| 快照自动刷新开关（`-Dupdate`） | 有了它，「评测红了」的最短路径变成「跑个命令让它变绿」，快照层会退化成耗时的空转。手工编辑的摩擦是特性不是缺陷 | 用例规模大到批量更新成为真实负担，且届时须同时配「说明未变更则失败」的守护——不能先给出口再补守护 |
| 精细诊断载体（仅内存、不落盘的未覆盖片段） | 它靠后人自觉维持隐私边界；半年后有人为排查方便把它塞进 trace，红线就破了且无测试报警 | 有机械约束能保证它不出现在 trace / 日志参数中 |
| LLM 自检器 | 生成 6.5s + 自检 6.5s + 重写 6.5s ≈ 19.5s，撞死 20s 超时；用不确定判定替换确定性 checker 是净损失 | 判定延迟降到亚秒级 |
| 向量库 / GraphRAG | 当前数据量下 SQL 检索足以验证产品命题；上云向量库会让隐私、成本、本地开发、测试夹具全部变重 | 相关性问题可被量化证明，且 `MemoryPort` 下可插拔接入 |
| MCP 内核化 | 工具是内生业务 API，无外部消费方；只增加转换层与安全面 | 需要对外暴露工具生态 |
| 精细诊断载体（带文本片段的护栏诊断） | 靠后人自觉维持隐私边界不可靠——半年后有人为排查方便塞进 trace，红线就破了且无测试报警；要做得配约束测试，成本反超收益 | 有类型层面而非纪律层面的保证手段 |
| Multi-Agent / 用户可见多角色 | 破坏「一个朋友」的产品隐喻，且无产品需求 | — |
| 主动推送 / 情绪 dashboard / STT | 与「被动召唤」和「不诊断」的产品气质直接冲突 | — |
| 平台升级到 Spring Boot 4.x | 重心不在平台层；风险性质与业务刀不同——业务刀的风险是「设计对不对」可由测试回答，平台升级的风险是「哪个传递依赖不兼容」，工作量长尾不可预估 | Phase 2 完工后，或某刀确实需要 4.x 特性 |

### 10.1 C9 时间智能：让旧记忆更安静，而不是把人变成数据

**一句话答案**：时间智能不是“分析用户”，而是让 Agent 知道一段记忆离现在有多远、越旧的旁支记忆越克制；只有用户主动比较且证据足够时，才允许问一句“似乎不止一次出现过，你觉得有关吗”。

**怎么做的**：backend 用固定 `Clock` 把记忆分为最近、数月前、更早与未知四档；旁支片段按
100% / 75% / 50% 缩小注入预算，但用户主动打开的回看目标记录始终保持主体地位。重复主题提示还要同时满足
回看模式、明确比较意图、两条不同旁支记录和至少 90 天跨度。策略全在本地确定性计算，零新增模型调用。

**为什么没有做趋势分析**：现有 SQL 命中只能证明有限线索重合，不能证明周期、因果、成长或退步。
因此量化、绝对频率、诊断、趋势与预测式话术由 `TEMPORAL_OVERREACH` 直接降级，也不进入反思重写；
产品侧不新增 dashboard、评分、提醒或“系统分析结果”。解释权必须留给用户。

**证据与边界**：fixed-clock 测试覆盖 30/31、180/181、null/future 与 90 天边界；真实 MySQL 用可清理合成数据验证
owner/status/focal 排除及时间衰减；真实 provider 固定合成探针 6/6 成功且均通过越界检查。eligible 场景的真实输出
没有实际采用 recurrence hint，因此不能声称提示采纳稳定；本机无可控微信真机环境，UI 活体证据仍为 SKIPPED。

---

## §11 踩过的坑

> 这一节是我认为最有价值的部分。能讲清一次真实事故的根因，比能背十个概念更说明问题。

### 11.1 H2 全绿不构成验证

轨迹落库在 H2 上 37 项测试全绿，却在真实 MySQL 上让每轮对话卡满 50 秒。H2 没有 InnoDB 行级锁语义，**该缺陷在 H2 上不可能复现**。

**改进**：凡涉及锁 / 外键 / 事务边界的改动，联调必须打真实 MySQL;写回归时优先断言**调用时机与不变量**而非结果，这样在 H2 上也能守住一部分。

### 11.2 `REQUIRES_NEW` 不等于「不影响外层事务」

它挂起外层事务，但**不释放外层已持有的锁**。新事务若要碰同一批行（尤其经由外键），就会与自己的外层事务死等。想做「业务提交后再做副作用」，该用事务同步回调，不是换传播级别。

### 11.3 前后端超时必须有明确先后，不能相等

两边都设 30s 时，前端总是先断，后端精心设计的显式失败语义被网络层错误覆盖，用户看到的是 `request: fail timeout`。定为前端 30s / 后端 20s,**顺序不可颠倒**。凡新增调用 AI 的前端请求，必须显式指定超时且大于后端上限。

### 11.4 规划期的「须同步 N 处」要在实现期复核

规划时我断言新表需要同步三份 schema，实现时发现全量脚本只维护到第一刀，项目既有约定与我的假设不符。**不要把假设写成事实。**

### 11.5 验证「拦截生效」前，先确认样本真的处于该被拦的状态

我曾取「最后一轮回复」做护栏剥离实验，而那一轮恰好没有复述历史内容，自然不该被拦。结论不翻转是**样本选错**，不是护栏失效。

### 11.6 主观感受会被无关变量污染

我曾判断「引导话术生硬」并准备去调 prompt。后来修掉一个 55 秒延迟缺陷，再体验就觉得「自然一些了」——而这期间**一行 prompt、一个阈值、一处引导策略都没改**。延迟污染了对语言质量的判断。

**这件事直接决定了我下一刀的顺序**：先建可回归的评测基线，再谈话术优化。否则只会得到第二个无法证伪的「感觉好了」。
