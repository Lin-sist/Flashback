# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*


Docs/学习文档

# Local M3 WeChat verification secrets
/backend/start-dev-wechat.local.ps1
/frontend/.env.local
/frontend/.env.*.local

# Local secrets moved out of start script (C2)。示例模板 secrets.local.env.example 需要入库，故显式放行。
/backend/secrets.local.env
!/backend/secrets.local.env.example
# 本地脚本备份与一次性验证脚本，可能含明文凭证
*.local.ps1.bak
# C5：一次性联调脚本会把凭证读进环境变量，一律不入库。
# 原先只点名 start-dev-wechat.local.ps1，新增的本地脚本不会被覆盖，故改为通配。
*.local.ps1
/backend/*.local.env
!/backend/*.local.env.example

# C6：Eval 用真实日记样本做输入时，样本一律不入库（用户日记是本仓库最高敏数据）。
# 用通配而非点名单个文件，理由同上面 C5 那条：点名时新增的样本文件不会被覆盖，
# 而这里漏出去的是用户日记原文。合成样例走 eval/cases/*.yaml，正常入库。
# 需要入库某个 .local.yaml 模板时按既有惯例显式放行（如 !*.example.local.yaml）。
*.local.yaml
*.local.yml

# Personal teaching / interview skills (not project delivery)
.agent/skills/Lincheck/
.agent/skills/Linsist/

/.claude
